/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // Uncommenting autoprefixer for proper CSS processing
  },
};

export default config;
